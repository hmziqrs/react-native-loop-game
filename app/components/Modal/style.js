import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    top: 0,
    left: 0,
    flex: 1,
    right: 0,
    bottom: 0,
    zIndex: 2,
    elevation: 2,
    position: 'absolute',
  },
});

export default styles;
